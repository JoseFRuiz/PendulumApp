<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp">

    <!-- Landscape layout: video takes the full height on the left, a narrow control
         column sits on the right, so the video gets as much of the screen as possible
         (see MainActivity's applyVideoTransform: it letterboxes to fit whatever space
         this view ends up with, so no fixed aspect ratio is assumed here). -->
    <TextureView
        android:id="@+id/videoView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginEnd="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/controlPanel" />

    <!-- Live angle/rate/error readout, driven by PendulumSpeedController's onTick callback.
         Gated behind BuildConfig.DEBUG in MainActivity. See python/pendulum_sim_process.md
         section 6: a controller that "looked plausible" from the video alone was once
         silently disengaged, only caught by watching its internal numbers. This is the
         Android equivalent of watching those numbers during bring-up. -->
    <TextView
        android:id="@+id/debugReadout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:text=""
        android:textSize="14sp"
        android:fontFamily="monospace"
        android:textColor="#FFFFFF"
        android:background="#CC000000"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:visibility="invisible"
        app:layout_constraintTop_toTopOf="@id/videoView"
        app:layout_constraintStart_toStartOf="@id/videoView" />

    <LinearLayout
        android:id="@+id/controlPanel"
        android:layout_width="140dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/statusText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/status_idle"
            android:textSize="14sp"
            android:textStyle="bold"
            android:layout_marginBottom="20dp" />

        <Button
            android:id="@+id/btnSelectVideo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/select_video"
            android:layout_marginBottom="20dp" />

        <Button
            android:id="@+id/btnStart"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/start"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/btnPause"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/pause"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/btnStop"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/stop" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
